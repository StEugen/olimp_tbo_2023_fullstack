import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#3F51B5',
        },
        secondary: {
            main: '#f50057',
        },
        background: {
            default: '#fff'
        }
    }
});
